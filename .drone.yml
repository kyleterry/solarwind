pipeline:
  docker:
    image: plugins/docker
    custom_dns: [ 1.1.1.1, 8.8.8.8 ]
  build:
    image: plugins/docker
    commands: [ make build ]
  
  publish:
    image: plugins/docker
    repo: kyleterry/solarwind
    tag: [ latest ]
    when:
      branch: master
      event: push
